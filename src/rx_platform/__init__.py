"""rx_platform — a UK Housing Data Platform built in stages.

This is the package's top-level file. It marks `src/rx_platform/` as a
Python package and exposes the version string.
"""

__version__ = "0.1.0"
